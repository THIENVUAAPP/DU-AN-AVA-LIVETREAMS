## Tiêu chí cập nhật phần mềm (Update Criteria)
- Mỗi lần code xong bắt buộc phải đóng gói (build) và cập nhật phiên bản mới nhất.
- Đảm bảo khi người dùng tải bản giải nén về và mở lên, phần mềm PHẢI luôn luôn hiển thị phiên bản mới nhất, không bao giờ bị dính cache phiên bản cũ.
