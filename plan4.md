# Kế hoạch điều chỉnh Giao diện Desktop & Kích hoạt Webcam

1. **Hiển thị Nhân vật & Nền**:
   - Thay đổi nền mặc định của khu vực hiển thị video thành màu trắng.
   - Cập nhật khu vực video để hiển thị ảnh/video của nhân vật được chọn (`selectedCharacter`).

2. **Xóa thông tin**:
   - Xóa `tungroup.vn` (đặt ID TikTok mặc định thành rỗng).
   - Xóa số Coin (`254,866,783,642`).
   - Xóa icon webcam thừa.

3. **Bố cục & Webcam (Tun Studio)**:
   - Sắp xếp lại bố cục thanh công cụ cho hợp lý hơn, đưa một số thành phần (như ID TikTok, kết nối) lên hàng trên cùng cho gọn.
   - Kích hoạt Webcam thực tế khi bấm vào nút "Tun Studio" (Yêu cầu quyền truy cập Camera và hiển thị luồng video từ Webcam lên một góc màn hình hoặc trong khu vực được chỉ định).
